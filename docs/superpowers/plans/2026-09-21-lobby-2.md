# Lobby 2 – Deck-Kacheln, Auswahl merken, Nochmal/Serie, Archidekt-Resync

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Startseite mit Deck-Kacheln (Commander-Bild) und Auswahl-Panel, gemerkte Deckauswahl, „Nochmal spielen" mit Serienstand/Best-of, Resync für Archidekt-Decks, dunkle Dropdown-Listen.

**Architecture:** Die Bridge liefert `DeckInfo` (Name, Commander mit `imageKey`, Archidekt-Id aus einem Deck-Tag) statt Namen und beantwortet `resyncDeck`. Im Web ersetzt `DeckPicker` (Kachel + Panel) die Dropdowns; reine Module `lobbyPicks.ts` (Auswahl merken), `series.ts` (Serie/Best-of) und `deckSearch.ts` (Filter) tragen die Logik, der Store hält `lastStart`/`series`. Spec: `docs/superpowers/specs/2026-09-21-lobby-2-design.md`.

**Tech Stack:** Java 21 (Bridge, JUnit 5, Jackson), Forge `Deck`/`DeckSerializer`, React 18, TypeScript, Vite, Vitest, Playwright, zustand.

## Global Constraints

- Branch `feature/lobby-2` (existiert). Commits deutsch, Kleinschreibung, Präfix `bridge:`/`ui:`/`test:`/`docs:`, Trailer `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`, keine weiteren Co-Authors.
- Kevins Bridge (Ports 8080/8081) nicht anfassen (sie läuft derzeit nicht); nie `mvn clean` in `bridge/`; ein Maven-Prozess gleichzeitig (Timeout 600000 ms); Bridge-Tests `cd bridge && mvn -q test -Dtest=<Klasse>`; Test-Ports 18081+.
- Web: `cd web && npm test`, `npm run build` grün; Layout-Check `npm run layout-check -- <url> fixtures/table.json` (1600x900) einmal am Ende grün (Overlay-Änderung).
- Screenshots: `npm run build`, eigene Bridge `cd bridge && mvn -q compile exec:java -Dmtgplayer.httpPort=18080 -Dmtgplayer.wsPort=18091` im Hintergrund (auf `curl … 18080` = 200 warten, am Ende die PID killen, nicht `pkill -f java`), URL `http://127.0.0.1:18080`; Dateien unter `/tmp/claude-1000/-home-kevin-projects-DiscordBot/4cee7c96-6aaf-442e-8cd8-a9f8632f3c2b/scratchpad/lobby2/`, jede mit Read ansehen und im Report beschreiben. Ein Vite-Dev-Server läuft evtl. noch auf 5173 (ohne Bilder, Proxy zeigt auf 8080) – nicht verwenden.
- Protokoll: `DeckInfo { name, commanders: [{ name, imageKey? }], archidekt?: string }`; `resyncDeck { name }`; Serie-Schlüssel = JSON der Deck-/KI-Angaben ohne Spielernamen; Best-of ∈ {0, 3, 5, 7}; localStorage-Schlüssel `mtg.lobby.picks` (Picks) – Best-of in `mtg.lobby.ai` (`AiSettings.bestOf`).
- Design: dunkel, ruhig (bestehende CSS-Variablen), Kachel-Art 56×78 px, Panel-Raster `repeat(auto-fill, minmax(150px, 1fr))`, `select`/`option` opak `#161d23`.

---

### Task 1: Bridge – `DeckInfo`, Archidekt-Tag, `resyncDeck`

**Files:**
- Modify: `bridge/src/main/java/mtgplayer/protocol/Messages.java` (`Lobby`, neue Records `DeckInfo`, `Commander`)
- Modify: `bridge/src/main/java/mtgplayer/forge/Precons.java` (`infos()`)
- Modify: `bridge/src/main/java/mtgplayer/decks/DeckStore.java` (`infos()`, `archidektId(name)`, Tag-Konstante)
- Modify: `bridge/src/main/java/mtgplayer/decks/DeckSource.java` (Tag setzen), `bridge/src/main/java/mtgplayer/server/Bridge.java` (`resyncDeck`, Lobby-Aufrufe)
- Test: `bridge/src/test/java/mtgplayer/decks/DeckStoreTest.java`, `DeckSourceTest.java`, `bridge/src/test/java/mtgplayer/server/BridgeEndToEndTest.java` (Lobby-Form), neu `bridge/src/test/java/mtgplayer/decks/DeckResyncTest.java`

**Interfaces:**
- Produces:
  ```java
  public record Commander(String name, String imageKey) { }
  public record DeckInfo(String name, List<Commander> commanders, String archidekt) { }   // archidekt null, wenn kein Archidekt-Import
  public record Lobby(String type, List<DeckInfo> precons, List<DeckInfo> decks, List<String> aiModes, List<String> aiProfiles, int aiTimeout)
  // Precons
  public static List<DeckInfo> infos()            // sortiert nach Name, einmal berechnet und gecacht
  public static DeckInfo info(String name, Deck d, String archidekt)   // Commander aus d.getCommanders(): name, getImageKey(false)
  // DeckStore
  public static final String ARCHIDEKT_TAG = "archidekt:";
  public List<DeckInfo> infos()                   // aus den Dateien, sortiert
  public String archidektId(String name)          // Id aus dem Tag oder null
  // DeckSource
  public Resolved resync(String name)             // wirft IllegalArgumentException("Resync <name>: …")
  ```
- JSON `{"type":"lobby","precons":[{"name":"…","commanders":[{"name":"…","imageKey":"c:…"}],"archidekt":null}],…}` – `archidekt` null lassen ist ok (Jackson Standard); das Web behandelt `null` wie fehlend.

- [ ] **Step 1: Failing Tests** – `DeckStoreTest` ergänzen:

```java
    @Test
    void infosLiefernCommanderMitBildUndArchidektTag(@TempDir Path dir) {
        DeckStore store = new DeckStore(dir);
        Deck d = Precons.load("Abzan Armor [TDC] [2025]");
        d.getTags().add(DeckStore.ARCHIDEKT_TAG + "12345");
        store.save("Mein Abzan", d);
        List<Messages.DeckInfo> infos = store.infos();
        assertEquals(1, infos.size());
        assertEquals("Mein Abzan", infos.get(0).name());
        assertEquals("Felothar the Steadfast", infos.get(0).commanders().get(0).name());
        assertTrue(infos.get(0).commanders().get(0).imageKey().startsWith("c:Felothar"));
        assertEquals("12345", infos.get(0).archidekt());
        assertEquals("12345", store.archidektId("Mein Abzan"));
        assertNull(store.archidektId("gibt es nicht"));
    }
```

`DeckSourceTest.archidektFormHoltParstUndSpeichert` erweitern: nach `r.save().run()` zusätzlich `assertEquals("1", store.archidektId("Fun With Fungus"));`. Neue Klasse `DeckResyncTest`:

```java
class DeckResyncTest {
    @BeforeAll static void boot() { ForgeBoot.init(); }

    @Test
    void resyncHoltNeuUndBehaeltNamenUndTag(@TempDir Path dir) throws Exception {
        String body = Files.readString(Path.of("src/test/resources/archidekt-1.json"));
        DeckStore store = new DeckStore(dir);
        DeckSource src = new DeckSource(store, new Archidekt(url -> body));
        src.resolve(Json.parse("{\"archidekt\":\"https://archidekt.com/decks/1/x\",\"deckName\":\"Pilze\"}")).save().run();
        // zweiter Fetch liefert dieselbe Liste mit einer Karte mehr (Text ersetzt) – hier reicht: Fetch wird gerufen
        int[] calls = {0};
        DeckSource src2 = new DeckSource(store, new Archidekt(url -> { calls[0]++; return body; }));
        DeckSource.Resolved r = src2.resync("Pilze");
        r.save().run();
        assertEquals(1, calls[0]);
        assertEquals(List.of("Pilze"), store.names());
        assertEquals("1", store.archidektId("Pilze"));
        assertEquals("Thelon of Havenwood", r.deck().getCommanders().get(0).getName());
    }

    @Test
    void resyncOhneTagMeldetFehler(@TempDir Path dir) {
        DeckStore store = new DeckStore(dir);
        store.save("Lokal", Precons.load("Abzan Armor [TDC] [2025]"));
        DeckSource src = new DeckSource(store, new Archidekt(url -> { throw new AssertionError("kein Fetch"); }));
        IllegalArgumentException e = assertThrows(IllegalArgumentException.class, () -> src.resync("Lokal"));
        assertTrue(e.getMessage().startsWith("Resync Lokal:"));
    }
}
```

`BridgeEndToEndTest`: die Stelle, die `lobby.get("precons")` als `List<String>` prüft (grep `precons`), auf die neue Form umstellen: erstes Element ist ein Objekt mit `name` und `commanders` (Array, Länge ≥ 1, erstes mit `imageKey`, das mit `c:` beginnt).

- [ ] **Step 2: Rot** – `cd bridge && mvn -q test -Dtest='DeckStoreTest,DeckSourceTest,DeckResyncTest'` → Kompilierfehler.

- [ ] **Step 3: Implementieren**
  - `Messages`: Records wie oben; `Lobby(List<DeckInfo> precons, List<DeckInfo> decks)`-Konstruktor wie bisher mit `AiConfig.modes()` etc.
  - `Precons.info(name, deck, archidekt)`: `deck.getCommanders()` → `new Commander(pc.getName(), pc.getImageKey(false))`; `infos()`: `names()` → `info(n, load(n), null)`, in einem `static volatile List<DeckInfo> cached` (unveränderlich) gecacht.
  - `DeckStore.infos()`: wie `names()` über die Dateien, je Datei `load(name)` und `Precons.info(name, deck, archidektId(deck))`; `archidektId(Deck)` liest `deck.getTags()` nach Präfix; `archidektId(String name)` lädt und delegiert, unbekannt → null. `save()` ändert nichts (Tags schreibt Forge). Hinweis: `load()` setzt `d.setName(name)` – Tags bleiben erhalten.
  - `DeckSource.resolve` (archidekt-Zweig): `resolveText(...)` bekommt die Id als dritten Parameter; vor `store.save` `r.deck().getTags().add(DeckStore.ARCHIDEKT_TAG + id)`. `resync(name)`: `String id = store.archidektId(name)`; null → `throw new IllegalArgumentException("Resync " + name + ": kein Archidekt-Deck")`; sonst `archidekt.fetch(id)` (Exceptions aus fetch als `IllegalArgumentException("Resync " + name + ": " + msg)` – prüfen, was `fetch` heute wirft, und einheitlich ummanteln), `resolveText(r.text(), name, id)`.
  - `Bridge`: `new Messages.Lobby(Precons.infos(), store.infos())` an beiden Stellen; `case "resyncDeck" -> GuiBase.getInterface().runBackgroundTask("resync", () -> { try { decks.resync(name).save().run(); ws.send(new Messages.Lobby(...)); } catch (IllegalArgumentException e) { ws.send(new Messages.ErrorMsg(e.getMessage())); } })`. `Bridge` benutzt `new DeckSource(store)` – prüfen, dass dieser Konstruktor `Archidekt.standard()` setzt (sonst ergänzen).

- [ ] **Step 4: Grün** – `mvn -q test -Dtest='DeckStoreTest,DeckSourceTest,DeckResyncTest,BridgeEndToEndTest'`; danach die volle Suite `mvn -q test` (Timeout 600000; erwartete ~170 Tests, 0 Fehler).

- [ ] **Step 5: Commit** – `bridge: deckinfo mit commander-bild in der lobby, archidekt-tag und resyncDeck`.

---

### Task 2: Web – Protokoll, Store (Serie), `lobbyPicks.ts`, `series.ts`, `deckSearch.ts`

**Files:**
- Modify: `web/src/protocol.ts`, `web/src/store.ts`, `web/src/aiSettings.ts` (`bestOf`), `web/fixtures/lobby.json`
- Create: `web/src/lobbyPicks.ts`, `web/src/series.ts`, `web/src/deckSearch.ts`
- Test: `web/src/lobbyPicks.test.ts`, `web/src/series.test.ts`, `web/src/deckSearch.test.ts`, `web/src/store.test.ts`, `web/src/aiSettings.test.ts`

**Interfaces:**
- Produces:
  ```ts
  // protocol.ts
  export interface DeckInfo { name: string; commanders: { name: string; imageKey?: string }[]; archidekt?: string | null }
  export interface Lobby { type: "lobby"; precons: DeckInfo[]; decks?: DeckInfo[]; aiModes?: string[]; aiProfiles?: string[]; aiTimeout?: number }
  export type StartGame = Extract<Outbound, { type: "startGame" }>;
  Outbound |= { type: "resyncDeck"; name: string }
  // series.ts
  export interface Series { key: string; wins: Record<string, number>; games: number }
  export function seriesKey(msg: StartGame): string          // JSON von { spectate, humanDeck, opponents ohne name }
  export function startSeries(prev: Series | undefined, msg: StartGame): Series   // gleicher key → prev, sonst { key, wins: {}, games: 0 }
  export function recordResult(s: Series, winner: string | null): Series
  export function seriesWinner(s: Series, bestOf: number): string | undefined      // Name mit wins >= ceil(bestOf/2), bestOf 0 → undefined
  export function formatSeries(s: Series, names: string[]): string                // "Du 2 · KI 1 1" (Reihenfolge names, fehlende 0)
  // lobbyPicks.ts
  export interface LobbyPicks { human: Pick; ais: Pick[] }
  export function loadPicks(getStorage: () => Pick<Storage, "getItem">): LobbyPicks   // Default { human: EMPTY, ais: [] }
  export function savePicks(getStorage: () => Pick<Storage, "setItem">, p: LobbyPicks): void
  export function dropMissing(p: LobbyPicks, precons: DeckInfo[], decks: DeckInfo[]): LobbyPicks  // precon/saved-Pick ohne passendes Deck → EMPTY
  // deckSearch.ts
  export function filterDecks(decks: DeckInfo[], query: string): DeckInfo[]   // Name oder Commander-Name enthält query (trim, case-insensitiv); leer → alle
  // aiSettings.ts
  export interface AiSettings { picks: AiPick[]; timeout: number; bestOf: 0 | 3 | 5 | 7 }   // Default 0, ungültig → 0
  // store.ts
  lastStart?: StartGame; series?: Series; bestOf: number (aus Lobby gesetzt, Default 0)
  noteStart(msg: StartGame): void   // setzt lastStart, series = startSeries(series, msg)
  resetSeries(): void
  gameOver-Reducer: series = series ? recordResult(series, winner ?? null) : series
  backToLobby: behält lastStart/series
  ```
- `EMPTY` Pick (`{ kind: "precon", value: "", name: "" }`) zieht von `Lobby.tsx` nach `deckref.ts` (`export const EMPTY_PICK`).

- [ ] **Step 1: Failing Tests** – `series.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { formatSeries, recordResult, seriesKey, seriesWinner, startSeries } from "./series";
import type { StartGame } from "./protocol";

const msg: StartGame = { type: "startGame", humanDeck: { precon: "A" }, opponents: [{ precon: "B", name: "KI 1" }] };

describe("series", () => {
  it("key ignoriert spielernamen", () => {
    const other: StartGame = { ...msg, opponents: [{ precon: "B", name: "Gegner" }] };
    expect(seriesKey(msg)).toBe(seriesKey(other));
    expect(seriesKey(msg)).not.toBe(seriesKey({ ...msg, opponents: [{ precon: "C", name: "KI 1" }] }));
  });
  it("startSeries behaelt eine serie mit gleichem key und setzt sonst zurueck", () => {
    const s = recordResult(startSeries(undefined, msg), "Du");
    expect(startSeries(s, msg)).toBe(s);
    expect(startSeries(s, { ...msg, humanDeck: { precon: "X" } })).toEqual({ key: seriesKey({ ...msg, humanDeck: { precon: "X" } }), wins: {}, games: 0 });
  });
  it("recordResult zaehlt siege und spiele", () => {
    let s = startSeries(undefined, msg);
    s = recordResult(s, "Du"); s = recordResult(s, "KI 1"); s = recordResult(s, "Du"); s = recordResult(s, null);
    expect(s.wins).toEqual({ Du: 2, "KI 1": 1 });
    expect(s.games).toBe(4);
  });
  it("seriesWinner ab ceil(n/2) siegen, bei bestOf 0 nie", () => {
    const s = { key: "k", wins: { Du: 2, "KI 1": 1 }, games: 3 };
    expect(seriesWinner(s, 3)).toBe("Du");
    expect(seriesWinner(s, 5)).toBeUndefined();
    expect(seriesWinner(s, 0)).toBeUndefined();
  });
  it("formatSeries in namensreihenfolge mit nullen", () => {
    expect(formatSeries({ key: "k", wins: { "KI 1": 1 }, games: 1 }, ["Du", "KI 1"])).toBe("Du 0 · KI 1 1");
  });
});
```

`lobbyPicks.test.ts` (Muster `aiSettings.test.ts`): rund-trip save/load mit Fake-Storage; kaputtes JSON → Default; ungültiger `kind` → EMPTY; `dropMissing` setzt einen `precon`-Pick auf EMPTY, dessen Name nicht in `precons` ist, lässt `text`/`archidekt` unberührt. `deckSearch.test.ts`: leere Query → alle; „felo" trifft Deck über Commander „Felothar the Steadfast"; „ABZAN" trifft „Abzan Armor"; kein Treffer → []. `store.test.ts`: bestehenden `lobby`-Test auf `DeckInfo` umstellen; neu: `gameOver` mit `winner: "Du"` nach `noteStart(msg)` zählt `series.wins.Du === 1`; `backToLobby` behält `series`. `aiSettings.test.ts`: `bestOf` rund-trip, ungültig (4) → 0.

- [ ] **Step 2: Rot** – `npm test` (Module fehlen).

- [ ] **Step 3: Implementieren** – Module wie in Interfaces; `store.ts`: `AppState` um `lastStart`, `series`, `bestOf` erweitern (`initialState.bestOf = 0`), Reducer `lobby`: `precons: m.precons, decks: m.decks ?? []` (Typen), `gameOver`: Serie fortschreiben; Store-Aktionen `noteStart`, `resetSeries`, `setBestOf`. `fixtures/lobby.json`: `precons`/`decks` als `DeckInfo` (Precons: `{"name":"Tinker Time (TDC)","commanders":[{"name":"…","imageKey":"c:…|TDC|1"}]}` – echte Commander-Namen/Keys aus `forge/forge-gui/res/quest/commanderprecons/<name>.dck` bzw. den `[Commander]`-Zeilen; `decks`: `{"name":"Felothar Landfall","commanders":[{"name":"Felothar the Steadfast","imageKey":"c:Felothar the Steadfast|TDC|1"}],"archidekt":"12345"}` und `Kalamax Storm` ohne `archidekt`).

- [ ] **Step 4: Grün** – `npm test`, `npm run build` (Lobby.tsx kompiliert noch, weil `precons: DeckInfo[]` dort in `<option>` als Objekt landet → in diesem Task minimal `x.name` verwenden; die echte Umstellung ist Task 3).

- [ ] **Step 5: Commit** – `ui: protokoll deckinfo, serie/best-of-logik, gemerkte deckauswahl (module + store)`.

---

### Task 3: `DeckPicker` (Kachel + Panel), Dropdown-Stil, Lobby-Integration, Screenshots

**Files:**
- Create: `web/src/components/DeckPicker.tsx`
- Modify: `web/src/components/Lobby.tsx`, `web/src/styles.css`, `web/scripts/shot-lobby.mjs`

**Interfaces:**
- Consumes: `DeckInfo`, `filterDecks`, `loadPicks`/`savePicks`/`dropMissing`, `EMPTY_PICK`, `AiSettings.bestOf`, `send({ type: "resyncDeck", name })`, `CardImage` (`imageKey` → `/img/…`).
- Produces: `DeckPicker({ pick, onChange, label }: { pick: Pick; onChange: (p: Pick) => void; label: string })`; CSS `.deck-tile`, `.deck-tile.empty`, `.deck-tile .art`, `.deck-tile .badge-src`, `.deck-panel`, `.deck-panel-backdrop`, `.deck-tabs`, `.deck-search`, `.deck-grid`, `.deck-card`, `.deck-card .resync`.

- [ ] **Step 1: `DeckPicker.tsx`**

```tsx
import { useEffect, useMemo, useRef, useState } from "react";
import type { DeckInfo } from "../protocol";
import type { Pick } from "../deckref";
import { filterDecks } from "../deckSearch";
import { useStore } from "../store";
import { send } from "../ws";
import CardImage from "./CardImage";

type Tab = "precons" | "saved" | "import";
const SRC_LABEL: Record<Pick["kind"], string> = { precon: "Precon", saved: "Eigenes Deck", text: "Textliste", archidekt: "Archidekt" };

/** Deckauswahl je Sitz: Kachel mit Commander-Art, Klick oeffnet das Panel (Reiter Precons / Eigene Decks / Import). */
export default function DeckPicker({ pick, onChange, label }: { pick: Pick; onChange: (p: Pick) => void; label: string }) {
  const precons = useStore((s) => s.precons);
  const decks = useStore((s) => s.decks);
  const [open, setOpen] = useState(false);
  const [tab, setTab] = useState<Tab>(pick.kind === "saved" ? "saved" : pick.kind === "precon" ? "precons" : "import");
  const [query, setQuery] = useState("");
  const [draft, setDraft] = useState<Pick>(pick);           // Import-Formulare
  const [syncing, setSyncing] = useState<string>();          // Deckname, dessen Resync laeuft
  const searchRef = useRef<HTMLInputElement>(null);
  const chosen: DeckInfo | undefined = pick.kind === "precon" ? precons.find((d) => d.name === pick.value)
    : pick.kind === "saved" ? decks.find((d) => d.name === pick.value) : undefined;
  useEffect(() => { if (open) { setQuery(""); setDraft(pick); searchRef.current?.focus(); } }, [open]);
  useEffect(() => { setSyncing(undefined); }, [decks]);      // neue lobby-Nachricht beendet "synchronisiert …"
  useEffect(() => {
    if (!open) return;
    const onKey = (e: KeyboardEvent) => { if (e.key === "Escape") setOpen(false); };
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  }, [open]);
  const list = useMemo(() => filterDecks(tab === "precons" ? precons : decks, query), [tab, precons, decks, query]);
  const choose = (kind: "precon" | "saved", d: DeckInfo) => { onChange({ kind, value: d.name, name: "" }); setOpen(false); };
  const title = chosen?.name ?? (pick.kind === "text" && pick.value.trim() ? (pick.name.trim() || "Textliste") : pick.kind === "archidekt" && pick.value.trim() ? (pick.name.trim() || pick.value.trim()) : undefined);
  return (
    <>
      <button type="button" className={"deck-tile" + (title ? "" : " empty")} onClick={() => setOpen(true)} aria-label={label}>
        <Art commanders={chosen?.commanders ?? []} />
        <span className="deck-tile-text">
          <span className="deck-tile-name">{title ?? "Deck wählen"}</span>
          {title && <span className="badge-src">{SRC_LABEL[pick.kind]}</span>}
          {chosen && <span className="deck-tile-cmd">{chosen.commanders.map((c) => c.name).join(" / ")}</span>}
        </span>
      </button>
      {open && (
        <div className="deck-panel-backdrop" onMouseDown={(e) => { if (e.target === e.currentTarget) setOpen(false); }}>
          <div className="deck-panel" role="dialog" aria-label={label}>
            <div className="deck-panel-head">
              <div className="deck-tabs">
                {(["precons", "saved", "import"] as Tab[]).map((t) => (
                  <button key={t} type="button" className={"tab" + (tab === t ? " on" : "")} onClick={() => setTab(t)}>
                    {t === "precons" ? "Precons" : t === "saved" ? "Eigene Decks" : "Import"}
                  </button>
                ))}
              </div>
              {tab !== "import" && <input ref={searchRef} className="deck-search" placeholder="Suchen (Deck oder Commander)" value={query} onChange={(e) => setQuery(e.target.value)} />}
              <button type="button" className="quiet small" onClick={() => setOpen(false)}>Schließen</button>
            </div>
            {tab !== "import" && (
              <div className="deck-grid">
                {list.length === 0 && <div className="muted">{tab === "saved" && decks.length === 0 ? "Noch keine eigenen Decks – über „Import" anlegen." : "kein Treffer"}</div>}
                {list.map((d) => (
                  <button key={d.name} type="button" className={"deck-card" + (chosen?.name === d.name && (pick.kind === (tab === "precons" ? "precon" : "saved")) ? " on" : "")}
                    onClick={() => choose(tab === "precons" ? "precon" : "saved", d)}>
                    <Art commanders={d.commanders} big />
                    <span className="deck-card-name">{d.name}</span>
                    <span className="deck-card-cmd">{d.commanders.map((c) => c.name).join(" / ")}</span>
                    {tab === "saved" && d.archidekt && (
                      <span className="resync" role="button" title={"Neu von Archidekt laden (" + d.archidekt + ")"}
                        onClick={(e) => { e.stopPropagation(); setSyncing(d.name); send({ type: "resyncDeck", name: d.name }); }}>
                        {syncing === d.name ? "synchronisiert …" : "↻ Resync"}
                      </span>
                    )}
                  </button>
                ))}
              </div>
            )}
            {tab === "import" && (
              <div className="deck-import">
                <label><input type="radio" checked={draft.kind !== "archidekt"} onChange={() => setDraft({ kind: "text", value: draft.kind === "text" ? draft.value : "", name: draft.name })} /> Textliste</label>
                <label><input type="radio" checked={draft.kind === "archidekt"} onChange={() => setDraft({ kind: "archidekt", value: draft.kind === "archidekt" ? draft.value : "", name: draft.name })} /> Archidekt-URL</label>
                {draft.kind === "archidekt" ? (
                  <div className="textdeck">
                    <input placeholder="https://archidekt.com/decks/12345/…" value={draft.value} onChange={(e) => setDraft({ ...draft, value: e.target.value })} />
                    <input placeholder="Name (optional, sonst Archidekt-Deckname)" value={draft.name} onChange={(e) => setDraft({ ...draft, name: e.target.value })} />
                  </div>
                ) : (
                  <div className="textdeck">
                    <input placeholder="Name (optional)" value={draft.name} onChange={(e) => setDraft({ ...draft, name: e.target.value })} />
                    <textarea rows={8} placeholder={"Archidekt/Arena-Export einfügen, z. B.\n1 Sol Ring (c21) 263\nCommander\n1 Felothar the Steadfast"} value={draft.value} onChange={(e) => setDraft({ ...draft, value: e.target.value })} />
                  </div>
                )}
                <p className="hint">Beim Spielstart wird das Deck gespeichert und erscheint danach unter „Eigene Decks" (Archidekt-Decks mit Resync).</p>
                <button type="button" className="primary" disabled={!draft.value.trim()} onClick={() => { onChange({ ...draft, kind: draft.kind === "archidekt" ? "archidekt" : "text" }); setOpen(false); }}>Übernehmen</button>
              </div>
            )}
          </div>
        </div>
      )}
    </>
  );
}

function Art({ commanders, big }: { commanders: DeckInfo["commanders"]; big?: boolean }) {
  return (
    <span className={"deck-art" + (big ? " big" : "") + (commanders.length > 1 ? " pair" : "")}>
      {commanders.length === 0 && <span className="deck-art-empty" />}
      {commanders.slice(0, 2).map((c) => <CardImage key={c.imageKey ?? c.name} imageKey={c.imageKey} className="art" />)}
    </span>
  );
}
```

(`draft.kind` startet mit `pick.kind`; öffnet man das Panel mit einem `precon`-Pick, gilt im Import-Reiter „Textliste" – der Radio setzt `kind` beim Wechsel.) Prüfen, wie `CardImage` bei fehlendem `imageKey` reagiert (Task-3-Report der Anhänge-Runde: gibt `null` zurück) – dann zeigt `.deck-art-empty` den Platzhalter.

- [ ] **Step 2: CSS** (Lobby-Block am Ende von `styles.css` erweitern; `select`-Regel Zeile ~93 anpassen):

```css
/* Native Dropdown-Listen: Chromium faerbt die aufgeklappte Liste nach der select-Hintergrundfarbe - mit
   rgba() faellt es (Windows) auf ein helles System-Popup zurueck, deshalb opak. */
select { background: #161d23; }
option { background: #161d23; color: var(--text); }

/* Deckauswahl: Kachel je Sitz + Panel (DeckPicker.tsx). */
.deck-tile { display: flex; align-items: center; gap: 12px; width: 100%; padding: 8px 10px; text-align: left; border-radius: var(--radius-md); border: 1px solid var(--line-2); background: rgba(0, 0, 0, 0.25); color: var(--text); cursor: pointer; }
.deck-tile:hover { border-color: var(--accent); background: rgba(79, 140, 255, 0.08); }
.deck-tile.empty { border-style: dashed; color: var(--muted); }
.deck-tile-text { display: flex; flex-direction: column; gap: 3px; min-width: 0; }
.deck-tile-name { font-weight: 600; font-size: 14px; overflow: hidden; text-overflow: ellipsis; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; }
.deck-tile-cmd { font-size: 12px; color: var(--muted); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.badge-src { align-self: flex-start; font-size: 10px; font-weight: 600; letter-spacing: 0.05em; text-transform: uppercase; padding: 1px 6px; border-radius: 999px; background: var(--accent-soft); color: var(--accent-2); }
.deck-art { position: relative; flex: none; width: 56px; height: 78px; border-radius: 5px; overflow: hidden; background: #0c1013; border: 1px solid var(--line-2); }
.deck-art .art { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; object-position: top; }
.deck-art.pair .art:first-child { clip-path: inset(0 50% 0 0); }
.deck-art.pair .art:last-child { clip-path: inset(0 0 0 50%); }
.deck-art-empty { position: absolute; inset: 0; background: repeating-linear-gradient(135deg, rgba(255,255,255,0.05) 0 6px, transparent 6px 12px); }
.deck-art.big { width: 100%; height: 110px; border-radius: 7px 7px 0 0; border: 0; }
.deck-panel-backdrop { position: fixed; inset: 0; z-index: 50; background: rgba(0, 0, 0, 0.55); display: flex; align-items: center; justify-content: center; padding: 24px; }
.deck-panel { width: min(960px, 100%); max-height: 85vh; display: flex; flex-direction: column; gap: 12px; padding: 16px; border-radius: 16px; background: #161d23; border: 1px solid var(--line-2); box-shadow: var(--shadow); }
.deck-panel-head { display: flex; gap: 12px; align-items: center; }
.deck-tabs { display: flex; gap: 4px; }
.deck-tabs .tab { padding: 6px 12px; border-radius: 999px; border: 1px solid transparent; background: transparent; color: var(--muted); font: inherit; cursor: pointer; }
.deck-tabs .tab.on { background: var(--accent-soft); border-color: rgba(79, 140, 255, 0.35); color: #fff; }
.deck-search { flex: 1; min-width: 0; }
.deck-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(150px, 1fr)); gap: 10px; overflow: auto; padding: 2px; }
.deck-card { position: relative; display: flex; flex-direction: column; gap: 4px; padding: 0 0 8px; text-align: left; border-radius: 8px; border: 1px solid var(--line); background: var(--panel); color: var(--text); font: inherit; cursor: pointer; overflow: hidden; transition: transform .12s ease, border-color .12s ease; }
.deck-card:hover { transform: translateY(-2px); border-color: var(--accent); }
.deck-card.on { border-color: var(--accent); box-shadow: 0 0 0 2px var(--accent-soft); }
.deck-card-name { padding: 6px 8px 0; font-weight: 600; font-size: 13px; line-height: 1.25; }
.deck-card-cmd { padding: 0 8px; font-size: 11px; color: var(--muted); }
.deck-card .resync { position: absolute; top: 6px; right: 6px; font-size: 11px; padding: 2px 8px; border-radius: 999px; background: rgba(8, 11, 14, 0.85); border: 1px solid var(--line-2); color: var(--text); }
.deck-card .resync:hover { border-color: var(--accent); color: #fff; }
.deck-import { display: flex; flex-direction: column; gap: 10px; }
.deck-import label { display: inline-flex; gap: 6px; align-items: center; margin-right: 16px; }
.deck-import .textdeck { display: flex; flex-direction: column; gap: 8px; }
.deck-import .primary { align-self: flex-start; }
```

Bestehende `.pick`/`.textdeck`-Regeln, die nur der alte Picker nutzte, entfernen, wenn nichts mehr darauf verweist (`grep -n "\.pick\b\|textdeck" web/src`).

- [ ] **Step 3: `Lobby.tsx`** – `picker()` durch `<DeckPicker pick={…} onChange={…} label="Dein Deck" />` bzw. `label={"KI " + (i+1)}` ersetzen; `EMPTY` → `EMPTY_PICK` aus `deckref.ts`. Picks laden/speichern: im Lade-Effekt (erste `lobby` mit `precons.length > 0`) `const p = dropMissing(loadPicks(() => localStorage), precons, decks); setHuman(p.human); setAis(restore…)` – die KI-Slot-Zahl kommt weiter aus `restoreSlots` (aiSettings); `ais` = `Array.from({length: picks.length}, (_, i) => p.ais[i] ?? EMPTY_PICK)`; Speichern in einem Effekt auf `[human, ais]` nach dem Laden (`savePicks`). Best-of: `const [bestOf, setBestOf] = useState<0|3|5|7>(0)`, aus `loadAiSettings(...).bestOf` laden, in `saveAiSettings` mitschreiben, `useStore.getState().setBestOf(bestOf)` bei Änderung; UI: in der Sektion „KI-Bedenkzeit" daneben ein `<label>Serie <select value={bestOf}>` mit Optionen `aus / Best of 3 / Best of 5 / Best of 7`. Start: `const start = () => { if (msg) { useStore.getState().noteStart(msg); send(msg); } }`. Serienstand unter dem Start-Knopf: `series && series.games > 0 && <div className="hint">Serie: {formatSeries(series, names)}{winner && ` – ${winner} hat die Serie gewonnen`} <button className="quiet small" onClick={resetSeries}>zurücksetzen</button></div>` mit `names = ["Du", ...ais.map((_, i) => "KI " + (i+1))]` (im Zuschauer-Modus ohne „Du").

- [ ] **Step 4: `shot-lobby.mjs`** – neue Flags `--open-panel` (klickt `.deck-tile` first), `--tab=<precons|saved|import>` (klickt `.deck-tabs .tab` mit dem Text), `--pick=<name>` (klickt im offenen Panel `.deck-card` mit diesem Namen, schließt damit). Das alte `--select` entfällt (kein Select mehr); Kommentar oben anpassen.

- [ ] **Step 5: Build + Screenshots** – `npm test`, `npm run build`; Bridge auf 18080 starten (Global Constraints), dann aus `web/`:
  - `node scripts/shot-lobby.mjs http://127.0.0.1:18080 <scratch>/lobby2/lobby-empty.png`
  - `… lobby-panel-precons.png --open-panel --tab=precons`
  - `… lobby-panel-saved.png --open-panel --tab=saved`
  - `… lobby-panel-import.png --open-panel --tab=import`
  - `… lobby-picked.png --open-panel --pick="Tinker Time (TDC)"`
  Jede ansehen: Kacheln mit Commander-Art (Bilder kommen über die Bridge von Scryfall – beim ersten Lauf evtl. noch nicht da, dann nach 5 s wiederholen), Panel dunkel, Raster lesbar, Resync-Knopf bei „Felothar Landfall", Import-Formular; ein aufgeklapptes native Select ist per Screenshot nicht prüfbar – die Regel aus Step 2 genügt. Nachbessern, bis es ruhig und lesbar wirkt (keine hellen Flächen, keine abgeschnittenen Namen).

- [ ] **Step 6: Commit** – `ui: deck-kacheln mit commander-bild und auswahl-panel, deckauswahl merken, best-of-einstellung, dunkle dropdowns`.

---

### Task 4: Spielende-Overlay „Nochmal spielen"/Serie, README

**Files:**
- Modify: `web/src/components/Table.tsx` (Overlay), `web/src/styles.css` (`.overlay .series`), `README.md`

- [ ] **Step 1: Overlay** – in `Table.tsx`:

```tsx
  const lastStart = useStore((s) => s.lastStart);
  const series = useStore((s) => s.series);
  const bestOf = useStore((s) => s.bestOf);
  const noteStart = useStore((s) => s.noteStart);
  const resetSeries = useStore((s) => s.resetSeries);
  const names = state.players.map((p) => p.name);
  const seriesWon = series ? seriesWinner(series, bestOf) : undefined;
  const again = () => {
    if (!lastStart) return;
    if (seriesWon) resetSeries();
    noteStart(lastStart);
    send(lastStart);
  };
  …
        <div className="overlay">
          <div className="dialog">
            <h3>{winner ? `${winner} gewinnt` : state.gameOver ? "Spiel beendet" : "Unentschieden"}</h3>
            {series && (series.games > 1 || bestOf > 0) && (
              <p className="series">Serie: {formatSeries(series, names)}{bestOf > 0 && ` · Best of ${bestOf}`}
                {seriesWon && <b> – {seriesWon} gewinnt die Serie</b>}</p>
            )}
            <div className="buttons">
              {lastStart && <button className="primary" onClick={again}>{seriesWon ? "Neue Serie" : "Nochmal spielen"}</button>}
              <button className={lastStart ? "quiet" : "primary"} onClick={backToLobby}>Zur Lobby</button>
            </div>
          </div>
        </div>
```

`noteStart` für eine gleiche Serie ist idempotent (`startSeries` gibt `prev` zurück). Nach `send(lastStart)` kommt der neue `state` (turn 0) und der Reducer leert `winner`? Prüfen: `reduce("state")` setzt `winner` nicht zurück – im `state`-Zweig bei `isNewMatch` zusätzlich `winner: undefined` setzen (Test in `store.test.ts`: nach `gameOver` und neuem `state` mit `turn: 0` ist `winner` undefined). Hinweis: `isNewMatch` verlangt `s.state === undefined` – beim Replay aus dem Overlay ist `s.state` noch gesetzt; Bedingung erweitern auf `m.turn === 0 && (s.state === undefined || s.winner !== undefined)` und den Test dafür ergänzen (Log wird geleert, `lastLogId` 0).

- [ ] **Step 2: CSS** – `.dialog .series { margin: 6px 0 0; color: var(--muted); font-size: 13px; }`.

- [ ] **Step 3: Screenshot** – Overlay: kleines Inline-Playwright-Skript im Scratchpad (Kopie von `shot.mjs`-Kopf): `table.json` anwenden, dann `window.mtgApply({type:"gameOver", winner:"Du"})`; vorher den Store per `window.mtgStore`? – nicht vorhanden: stattdessen im Skript nach dem `gameOver` per `page.evaluate` prüfen, dass `.dialog` sichtbar ist, und Screenshot `<scratch>/lobby2/overlay.png` (ohne Serie, da `lastStart` fehlt – der „Zur Lobby"-Knopf ist dann primär). Für die Serie-Zeile ein Unit-Test statt Screenshot (`series.test.ts` deckt `formatSeries`/`seriesWinner`).
  Layout-Check `npm run layout-check -- http://127.0.0.1:18080 fixtures/table.json` (1600x900) grün.

- [ ] **Step 4: README** – Abschnitt Lobby/Start: Deck-Kacheln (Commander-Bild, Panel mit Precons/Eigene Decks/Import, Suche), Auswahl bleibt gemerkt, „Nochmal spielen" im Spielende-Dialog mit Serienstand und Best-of (Lobby), Resync für Archidekt-Decks (Herkunft als Tag `archidekt:<id>` in der `.dck`-Datei). Protokollabschnitt: `lobby` mit `DeckInfo`, `resyncDeck`.

- [ ] **Step 5: Tests/Build/Commit** – `npm test`, `npm run build`; Commit `ui: nochmal spielen und serienstand im spielende-dialog, readme lobby 2`.
